(ns clojure-bidi-ring-rest-api-template.core-test
  (:require [clojure.test :refer :all]
            [clojure-bidi-ring-rest-api-template.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
